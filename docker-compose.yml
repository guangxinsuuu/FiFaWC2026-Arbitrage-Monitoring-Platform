services:
  wc2026-opportunity-engine:
    build: .
    container_name: wc2026-opportunity-engine
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
